import system from 'system-components';

export const Section = system({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  position: 'relative',
  maxWidth: '100rem',
  width: '100vw',
  m: '0 auto',
});
